# C-1.2.0 验证日志

## 1. 本轮验证结论总览

- `UNO`：源码已补丁、两套构建链路均通过编译
- `J(OpenMV)`：实验副本与协议辅助脚本已落仓，已完成 Python 语法检查
- `F411`：桥接状态机和协议源码骨架已落仓，并已在你提供的 STM32 工具链路径下产出 `.elf/.hex`；烧录/LED/UART 仍未执行
- `F411`：已新增 `PB2` 心跳灯最小存活验证逻辑，当前优先观察板子是否跑起来
- 全链路上板验证：未执行

## 2. Gate0：F411 板卡可用性闸门

### 2.1 目标

- CubeIDE 新建工程可编译
- 可烧录
- LED/GPIO 心跳可跑
- 至少一组 UART 回环成功

### 2.2 本轮结果

- 状态：`PARTIAL`
- `tools/build_f411_bridge.ps1`：通过
- 工具链路径：`F:\CodeForge\STM32CubeIDE_2.1.0\STM32CubeIDE`
- 已产物：
  - `artifacts/C-1.2.0/f411/cj_bridge_f411.elf`
  - `artifacts/C-1.2.0/f411/cj_bridge_f411.hex`
  - `artifacts/C-1.2.0/f411/cj_bridge_f411.map`
- 本轮新增最小观察项：
  - `PB2` 心跳灯
  - 接法：LED 阴极接 `PB2`，LED 阳极经限流电阻接 `3V3`
  - 现象：约 `100 ms` 亮、`900 ms` 灭，循环重复
- 未完成：
  - 实板烧录
  - UART 回环
- `F103` 回退：`未触发`

## 3. Gate1：UNO ↔ F411 逻辑入口

### 3.1 编译验证

- `tools/compile_uno.ps1`：通过
- `tools/compile_uno_cli.ps1`：通过

### 3.2 本轮已落地逻辑

- `bridgeSerial(A3/A4)` 已接入
- `STOP_REQ -> STOP_ACK`
- `RESUME_REQ -> RESUME_ACK`
- `PING -> PONG`
- `externalPauseActive` 会在暂停期间强制底盘停车

### 3.3 说明

- 本轮只验证代码与构建，不等同于实板串口往返已完成
- `CJ_COMM_POC_BUILD=1` 时，HC-06 接收路径不在验收范围内

## 4. Gate2：F411 ↔ J(OpenMV)

### 4.1 本轮已落地内容

- `OpenMV UART1(A9/A10)` 作为协议端点已冻结
- `cj_link.py` 已落仓，支持：
  - `COLOR_FOUND`
  - `ARM_BUSY`
  - `PICK_DONE`
  - `PICK_TIMEOUT`
  - `ARM_FAIL`
  - `HEARTBEAT`
  - `PICK_WINDOW / ABORT / PING` 接收解析
- `main.py` 已将抓取动作前增加 `WAIT_PICK_WINDOW` 状态门

### 4.2 当前状态

- 状态：`CODE_READY / HARDWARE_PENDING`
- 未执行真实串口往返与时序观测

## 5. Gate3：全链路真实闭环

目标场景：

`COLOR_FOUND -> STOP_REQ -> STOP_ACK -> PICK_WINDOW -> PICK_DONE/PICK_TIMEOUT -> RESUME_REQ -> RESUME_ACK`

当前状态：`PENDING`

原因：

- `F411 Gate0` 未过
- 未进行三板上电与接线验证

## 6. Gate4：10 轮重复性

当前状态：`PENDING`

待记录字段：

- 成功次数
- 停车是否可靠
- 恢复是否可靠
- 超时是否可靠
- 是否出现串口卡死
- 是否出现状态机跑飞
- 最终使用 `F411` 还是 `F103`
- 最终实际 UART 引脚与电平假设

## 7. 当前已完成的本机检查

### 7.1 UNO 构建

- `arduino-builder`：通过
- `arduino-cli`：通过
- 结果（本轮修改后）：
  - Flash 约 `7.7 KB`
  - RAM `352 B`

### 7.2 J 侧脚本语法

- `jixiebi/experiments/C-1.2.0_color_grasp_uart_poc/cj_link.py`：`python -m py_compile` 通过
- `jixiebi/experiments/C-1.2.0_color_grasp_uart_poc/main.py`：`python -m py_compile` 通过
- `jixiebi/experiments/C-1.2.0_color_grasp_uart_poc/pid.py`：`python -m py_compile` 通过

### 7.3 F411 最小产物构建

- `tools/build_f411_bridge.ps1`：通过
- 说明：
  - 当前 `F411` 产物已包含 `PB2` 心跳灯和 `SysTick` 毫秒节拍
  - 当前 `F411` 产物仍未包含真实 `USART1/USART2` 初始化
  - 链接阶段存在 `nosys` 相关 `_read/_write/_close/_lseek` warning，但不阻塞 `.elf/.hex` 生成

## 8. 下一步

1. 在具备 `STM32CubeIDE` 的机器上补齐 `F411 Gate0`
2. 连线执行 `UNO <-> F411` 的 `PING/STOP/RESUME` 往返测试
3. 再执行 `F411 <-> OpenMV` 的 `COLOR_FOUND / PICK_WINDOW / PICK_DONE` 往返测试
4. 最后跑 10 轮闭环并回填本日志
