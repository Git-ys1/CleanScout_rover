# C-3.0.5：OpenRF1 四路通道真值表

## 1. 当前固定观察对象

本轮只记录 `CN1~CN4` 的两类真值：

- 电机驱动方向真值
- 编码器方向真值

当前物理轮位注释先按已知实测写入：

| 通道 | 当前物理轮位注释 |
| --- | --- |
| `CN1` | 左后 |
| `CN2` | 左前 |
| `CN3` | 右后 |
| `CN4` | 右前 |

## 2. 当前 bringup 初始配置

| 通道 | motor_dir_sign | encoder_dir_sign |
| --- | --- | --- |
| `CN1` | `-1` | `+1` |
| `CN2` | `+1` | `+1` |
| `CN3` | `-1` | `+1` |
| `CN4` | `+1` | `+1` |

说明：

- 这只是 `C-3.0.5` bringup 初值。
- 当前已确认：这组初值没有建立起统一的单通道正反语义，不能直接作为后续实现真值。
- `C-3.0.5B` 只把这里当作失败回溯点，不再沿用其方向修正思路。

## 3. 回填表

### 驱动方向真值

| 通道 | `M,ch,+700` 实际结果 | `M,ch,-700` 实际结果 | 当前判断 |
| --- | --- | --- | --- |
| `CN1` | 反转 | 不转 | 正负语义未建立，不能回填 |
| `CN2` | 不转 | 正转 | 正负语义未建立，不能回填 |
| `CN3` | 正转 | 不转 | 正负语义未建立，不能回填 |
| `CN4` | 不转 | 反转 | 正负语义未建立，不能回填 |

当前阶段结论：

- `CN1~CN4` 还没有形成“`+pwm` = 统一语义正向、`-pwm` = 统一语义反向”的底层真值。
- 当前更接近“每通道只有一个方向能稳定转，且不同通道的有效符号不一致”。
- 因此本轮不能继续靠直接修改 `g_csr_motor_dir_sign` 试错，必须进入 `C-3.0.5B` 的单通道真值重建。

### 编码器方向真值

| 通道 | 正转 `delta` 符号 | 反转 `delta` 符号 | 是否需调整 `encoder_dir_sign` |
| --- | --- | --- | --- |
| `CN1` | 待测 | 待测 | 待测 |
| `CN2` | 待测 | 待测 | 待测 |
| `CN3` | 待测 | 待测 | 待测 |
| `CN4` | 待测 | 待测 | 待测 |

## 4. 当前测试流程

1. 串口上电后确认 `CSR_RF1_READY`
2. 单通道驱动测试：
   - `M,1,300` / `M,1,-300`
   - `M,2,300` / `M,2,-300`
   - `M,3,300` / `M,3,-300`
   - `M,4,300` / `M,4,-300`
3. 每步测试后执行：
   - `STOP`
4. 编码器查询：
   - `E,1` / `E,2` / `E,3` / `E,4`
5. 将结果回填到本表，不再靠口头记忆

## 5. C-3.0.5 失败回溯说明

- 当前最小协议已经打通：
  - `CSR_RF1_READY`
  - `ACK:M`
  - `ACK:E`
  - `ACK:STOP`
- 当前问题不在串口协议，而在单通道底层真值未建立。
- `C-3.0.5B` 的施工原则是：
  - 保留这份文档作为失败证据
  - 新分支只重建单通道真值
  - 在真值完整前，不回填方向表，不恢复高层语义
