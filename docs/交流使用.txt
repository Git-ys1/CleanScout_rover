当前树莓派端新的建图 / 导航操作口径如下，旧的 `/csr_base/ack` 和旧 UNO wrapper 口径不要再用了：

零、清理脚本
bash /home/clbrobot/Work/CleanScout_rover/Raspberrypi/catkin_ws/clean_mapping_nav_sessions.sh

云端后端参数：
edge_url=wss://api.hzhhds.top/edge/ros
edge_device_id=csrpi-001
edge_device_token=沿用当前 edge token

当前唯一验收通过并允许继续使用的成熟节点口径如下：

1. 多功能：`clbrobot/launch/demo/multi_function_demo.launch`
2. 导航：`bash ./start_desk_navigation.sh`
3. 如果导航排障，则使用本文件里的“手动逐层启动方式”

不允许把未验证的新节点、新脚本直接当正式入口替换这两个成熟入口。

多功能节点当前 edge-relay 已支持：

1. 优先连接云端后端：`wss://api.hzhhds.top/edge/ros`
2. 如果连续 3 次连接失败，则自动切换到本地后端：`ws://10.156.250.190:3000/edge/ros`
3. 当前不需要后端改协议，只是树莓派端增加了连接优先级切换

当前多功能 demo 推荐启动方式：

cd /home/clbrobot/Work/CleanScout_rover/Raspberrypi/catkin_ws
source ./use_cleanscout_pi.sh
roslaunch /home/clbrobot/Work/CleanScout_rover/Raspberrypi/catkin_ws/src/clbrobot_project/clbrobot/launch/demo/multi_function_demo.launch \
  enable_edge_relay:=true \
  edge_device_id:=csrpi-001 \
  edge_device_token:=ac27b6d55f9446daae792bccbb51df4438da3c88d7f9d74986276da8898e66d2 \
  edge_url:=wss://api.hzhhds.top/edge/ros \
  edge_fallback_url:=ws://10.156.250.190:3000/edge/ros \
  edge_primary_failures_before_fallback:=3

如果默认值已经写死完成，也可以直接启动：

cd /home/clbrobot/Work/CleanScout_rover/Raspberrypi/catkin_ws
source ./use_cleanscout_pi.sh
roslaunch /home/clbrobot/Work/CleanScout_rover/Raspberrypi/catkin_ws/src/clbrobot_project/clbrobot/launch/demo/multi_function_demo.launch

观察日志时，如果看到类似下面口径，说明已经自动切本地：

`edge relay primary endpoint failed 3 times, switching to fallback ws://10.156.250.190:3000/edge/ros`

当前多功能节点是**已验证通过**的节点：

1. 底盘遥控相关能力通过它验证
2. 风机 / 继电器 / 舵机联动通过它验证
3. 后端当前如果要联调多功能，只允许继续使用它


一、先做一次当前工作区编译刷新

在树莓派本机执行：

cd /home/clbrobot/Work/CleanScout_rover/Raspberrypi/catkin_ws
bash -lc 'cd /home/clbrobot/Work/CleanScout_rover/Raspberrypi/catkin_ws && catkin_make'

说明：

1. 本轮目标是刷新当前工作区 overlay
2. 不要反复重编
3. 如果编译失败，把最后报错写回本文件

二、406 建图正式入口

在树莓派本机执行：

cd /home/clbrobot/Work/CleanScout_rover/Raspberrypi/catkin_ws
export EDGE_DEVICE_ID=csrpi-001
export EDGE_DEVICE_TOKEN=ac27b6d55f9446daae792bccbb51df4438da3c88d7f9d74986276da8898e66d2
export EDGE_RELAY_URL=ws://10.63.109.190:3000/edge/ros
bash ./run_slam_mapping.sh

十二、导航链手动逐层启动方式（用于直接看每层报错）

如果 `start_desk_navigation.sh` 太黑盒，当前不要先跑脚本，改用手动逐层启动。

每个新终端都先执行：

cd /home/clbrobot/Work/CleanScout_rover/Raspberrypi/catkin_ws
source ./use_cleanscout_pi.sh

0. 先清理旧会话：

bash ./clean_mapping_nav_sessions.sh

1. 终端 1：启动 roscore

roscore

2. 终端 2：启动最小底盘链

roslaunch /home/clbrobot/Work/CleanScout_rover/Raspberrypi/catkin_ws/src/clbrobot_project/clbrobot/launch/bringup_rf1_min.launch

确认：

rostopic list | grep -E "/rf1/vel|/rf1/status|/rf1/wheel_target_ms|/rf1/cmdvel_debug"

3. 终端 3：启动 IMU

roslaunch /home/clbrobot/Work/CleanScout_rover/Raspberrypi/catkin_ws/src/clbrobot_project/clbrobot/launch/core/imu_only.launch

确认：

rostopic list | grep /imu/data

4. 终端 4：启动雷达

roslaunch /home/clbrobot/Work/CleanScout_rover/Raspberrypi/catkin_ws/src/clbrobot_project/clbrobot/launch/lidar/rplidar.launch

确认：

rostopic list | grep /scan

5. 终端 5：启动 laser_scan_matcher

roslaunch /home/clbrobot/Work/CleanScout_rover/Raspberrypi/catkin_ws/src/clbrobot_project/clbrobot/launch/slam/laser_scan_matcher_406.launch

确认：

rostopic list | grep /odom_lsm

6. 终端 6：启动导航主链

roslaunch /home/clbrobot/Work/CleanScout_rover/Raspberrypi/catkin_ws/src/clbrobot_project/clbrobot/launch/nav/navigation_406_rf1.launch

确认：

rostopic list | grep -E "/amcl_pose|/move_base/status|/cmd_vel"

建议额外开观察终端：

终端 7：

rostopic echo /odom_lsm

终端 8：

rostopic echo /amcl_pose

终端 9：

rostopic echo /move_base/status

终端 10：

rostopic echo /cmd_vel

终端 11：

rostopic echo /rf1/cmdvel_debug

终端 12：

rostopic echo /rf1/wheel_target_ms

如果还要在虚拟机 / 上位机看 RViz：

1. Fixed Frame = `map`
2. 先 `2D Pose Estimate`
3. 再 `2D Nav Goal`

当前手动正常启动顺序等价于：

1. `roscore`
2. `bringup_rf1_min.launch`
3. `imu_only.launch`
4. `rplidar.launch`
5. `laser_scan_matcher_406.launch`
6. `navigation_406_rf1.launch`

如果某一层直接报错，不要跳步，直接把那个终端的原始报错贴回来。


当前正式建图默认口径已经切到：

`/scan -> laser_scan_matcher -> /odom_lsm -> gmapping`

如果成功，之后到上位机 / 虚拟机 RViz 开始建图。

三、406 地图保存

在树莓派本机执行：

cd /home/clbrobot/Work/CleanScout_rover/Raspberrypi/catkin_ws
bash ./save_map.sh

默认保存位置已改为：

1. `$(find clbrobot)/maps/406.yaml`
2. `$(find clbrobot)/maps/406.pgm`

不再默认保存成：

1. `lab_map_001`
2. `desk_map_001`

四、406 导航正式入口

在树莓派本机执行：

cd /home/clbrobot/Work/CleanScout_rover/Raspberrypi/catkin_ws
bash ./start_desk_navigation.sh

这个脚本当前按以下顺序分阶段启动：

1. source `use_cleanscout_pi.sh`
2. 清理旧会话 `clean_mapping_nav_sessions.sh`
3. 显式启动 `roscore`
4. 启动 `bringup_rf1_min.launch`
5. 等待 `/rf1/vel`
6. 启动 `core/imu_only.launch`
7. 等待 `/imu/data`
8. 启动 `lidar/rplidar.launch`
9. 等待 `/scan`
10. 启动 `slam/laser_scan_matcher_406.launch`
11. 等待 `/odom_lsm`
12. 启动 `nav/navigation_406_rf1.launch`
13. 等待 `/odom_lsm`
14. 等待 `/amcl_pose`
15. 等待 `/move_base/status`

默认导航地图已改为：

`$(find clbrobot)/maps/406.yaml`

不再继续默认：

`/home/clbrobot/Work/CleanScout_rover/Raspberrypi/maps/desk_map_001.yaml`

当前导航入口 `bash ./start_desk_navigation.sh` 是**已验证通过**的正式入口：

1. 虚拟机 / 上位机 RViz 导航继续使用它
2. 当前不要换成别的“导航 demo 节点”或未验证联合脚本
3. 如果需要定位/排障，只能退回本文件里的“手动逐层启动方式”来查

五、虚拟机 / 上位机查看建议

source /opt/ros/noetic/setup.bash
rostopic list
rviz

RViz 里建议先看：

1. Fixed Frame = map
2. Map
3. LaserScan = /scan
4. TF
5. ParticleCloud = /particlecloud

导航时先 2D Pose Estimate，再 2D Nav Goal。

六、当前必须知道的现实风险

1. 旧地图确实是通过 gmapping SLAM 链建出来的
2. 当前雷达运行时仍可能依赖旧 `~/catkin_ws` 的已编译 `rplidarNode`
3. 如果后续建图 / 导航仍被雷达挡住，就要考虑把 `rplidar_ros` 在当前工作区正式重新编译并切正

七、如果异常，不要反复重启，先回报下面状态

建图链重点回报：

1. `/rf1/vel` 是否存在
2. `/imu/data` 是否存在
3. `/scan` 是否存在
4. `/map` 是否存在

导航链重点回报：

1. `/odom_lsm` 是否存在
2. `/scan` 是否存在
3. `/amcl_pose` 是否存在
4. `/move_base/status` 是否存在

九、导航与多功能 当前允许的联合演示口径

当前“导航与多功能”不是一个新的独立正式节点，也不是要替换现有入口。

当前允许的联合演示方式，必须基于上面已经验收通过的两个成熟入口：

1. 导航仍然使用：`bash ./start_desk_navigation.sh`
2. 多功能仍然使用：`clbrobot/launch/demo/multi_function_demo.launch`

当前联合演示分工固定为：

1. 导航：虚拟机 / 上位机 RViz 控制
2. 多功能：后端控制风机 / 继电器 / 舵机联动
3. 当前后端不接管导航控制

一句话：

当前“导航与多功能”联合演示，是**两个成熟入口并行运行**，不是换成一个未经验证的新节点。

十、当前要启动的节点名字（最短口径）

1. 多功能正式节点：`multi_function_demo.launch`

启动命令：

cd /home/clbrobot/Work/CleanScout_rover/Raspberrypi/catkin_ws
source ./use_cleanscout_pi.sh
roslaunch /home/clbrobot/Work/CleanScout_rover/Raspberrypi/catkin_ws/src/clbrobot_project/clbrobot/launch/demo/multi_function_demo.launch

2. 导航正式入口：`start_desk_navigation.sh`

启动命令：

cd /home/clbrobot/Work/CleanScout_rover/Raspberrypi/catkin_ws
bash ./start_desk_navigation.sh

3. 导航与多功能联合演示脚本：`run_nav_and_multifunction_demo.sh`

启动命令：

cd /home/clbrobot/Work/CleanScout_rover/Raspberrypi/catkin_ws
bash ./run_nav_and_multifunction_demo.sh

一句话：

1. 平时多功能只起 `multi_function_demo.launch`
2. 平时导航只起 `start_desk_navigation.sh`
3. 要联合演示时才起 `run_nav_and_multifunction_demo.sh`

八、新版可前端控制建图版本

当前 `run_slam_mapping.sh` 已改为默认启动 `edge-relay`。

这意味着建图时不再需要人手拖动小车，而是可以直接走：

前端按钮 -> 后端 -> edge-relay -> `/cmd_vel` -> RF1 -> 小车运动

本轮目标不是再起旧 Web 总栈，也不是另写第二套遥控 ROS，而是把现有 `edge-relay` 直接并入当前正式建图主链。

执行方式如下：

在树莓派本机执行：

cd /home/clbrobot/Work/CleanScout_rover/Raspberrypi/catkin_ws
export EDGE_DEVICE_ID=csrpi-001
export EDGE_DEVICE_TOKEN=ac27b6d55f9446daae792bccbb51df4438da3c88d7f9d74986276da8898e66d2
export EDGE_RELAY_URL=ws://10.22.7.190:3000/edge/ros
bash ./run_slam_mapping.sh

说明：

1. 当前脚本默认会启动 `edge-relay`
2. 不再需要额外加 `--with-edge-relay`
3. 只有明确想关闭时，才使用：`bash ./run_slam_mapping.sh --without-edge-relay`
4. 建图主链仍然是当前正式主链：RF1 + odom + IMU + lidar + gmapping
5. `edge-relay` 只是作为当前建图阶段的远程控制入口挂入主链，不另起第二套 ROS master

当前预期链路：

1. `run_slam_mapping.sh` 拉起当前正式建图链
2. 同时启动 `edge-relay`
3. 前端按钮经后端下发控制
4. `edge-relay` 发布 `/cmd_vel`
5. `cmdvel_to_rf1.py` 和 `rf1_serial_bridge.py` 驱动 OpenRF1
6. 小车可被前端控制移动完成建图

当前重要日志文件：

1. 建图日志：`/tmp/c331_mapping_406.log`
2. RF1 最小链日志：`/tmp/c331_rf1_min.log`
3. edge-relay 日志：`/tmp/c331_edge_relay.log`


如果以后改云端，只需要切换：

export EDGE_RELAY_URL=ws://10.22.7.190:3000/edge/ros

然后继续执行：

bash ./run_slam_mapping.sh
