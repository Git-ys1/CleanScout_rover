# C-2.3.5 Runtime Truth

## 文档定位

- 任务：`C-2.3.5A`
- 目标：冻结树莓派端建图/保存/定位导航的唯一入口，并把旧会话污染问题产品化清理

## 当前唯一入口冻结

### 1. 唯一建图入口

当前唯一建图入口冻结为：

```text
clbrobot/launch/slam/lidar_slam_pi.launch
```

当前推荐启动包装脚本：

```text
Raspberrypi/catkin_ws/start_slam_mapping.sh
Raspberrypi/catkin_ws/run_slam_mapping.sh
```

要求：

1. `start_slam_mapping.sh` 为唯一推荐的人手启动入口
2. `run_slam_mapping.sh` 为兼容脚本，但同样只能通过**单个 roslaunch**启动整套会话
3. 禁止再先手工起 `roscore`，再叠加第二套 `roslaunch`
4. 禁止再在脚本里手工补起第二个 master

### 2. 唯一地图保存入口

当前唯一保存入口冻结为：

```text
Raspberrypi/catkin_ws/save_map.sh
```

它只做一件事：

1. 保存当前 `/map`
2. 把地图存到固定目录：

```text
Raspberrypi/maps/
```

### 3. 唯一定位/导航入口

当前唯一定位/导航骨架冻结为：

```text
clbrobot/launch/navigate.launch
```

说明：

1. 它仍是已知地图定位与导航的唯一骨架
2. 下一阶段若要正式进入 AMCL 与 2D Nav Goal，优先在它上面收口，不另拼散入口

## 当前禁止直接混用的旧入口

以下入口仍保留，但当前禁止与新包装路径混用：

1. `clbrobot/launch/bringup.launch`
2. 旧 `rosserial_python + /dev/clbbase` 直连路径
3. 任何未清理旧会话后直接再次起的 `roscore/roslaunch`

## 当前必须执行的清理动作

本轮新增会话清理脚本：

```text
Raspberrypi/catkin_ws/clean_ros_sessions.sh
```

它负责：

1. 清旧 `roscore`
2. 清旧 `rosmaster`
3. 清旧 `roslaunch`
4. 清 `slam_gmapping`
5. 清 `move_base`
6. 清传感器与底盘桥残留节点

这是当前树莓派端必须执行的启动前动作，用来避免：

1. `11311` 端口冲突
2. 同名节点重复注册
3. 旧会话污染新建图会话

## 后续长期开发硬约束

从本轮开始，树莓派长期开发必须遵守以下硬约束：

### 1. 禁止在 `~/.bashrc` 中自动 source 项目工作区

只允许保留系统级：

```text
source /opt/ros/noetic/setup.bash
```

禁止再把以下路径设为默认自动 source：

1. `~/goolge_ws/install_isolated/setup.bash`
2. `~/catkin_ws/devel/setup.bash`
3. 任意 `CleanScout` 项目工作区 setup.bash

### 2. 禁止在 `~/.bashrc` 中自动导出项目级 ROS 变量

禁止把以下变量设为全局 shell 默认值：

1. `ROS_MASTER_URI`
2. `ROS_IP`
3. `ROS_HOSTNAME`
4. `CLBBASE`
5. `CLBLIDAR`

这些变量只能通过项目入口脚本设置。

### 3. 必须通过新工作区入口进入项目环境

统一入口：

```text
Raspberrypi/catkin_ws/use_cleanscout_pi.sh
```

任何需要进入项目环境的 shell，都应显式执行：

```bash
source /home/clbrobot/Work/CleanScout_rover/Raspberrypi/catkin_ws/use_cleanscout_pi.sh
```

### 4. 禁止后台遗留 master 成为默认操作方式

禁止长期依赖：

1. `nohup ... roscore &`
2. `nohup ... rosmaster --core &`
3. 任意未登记、未清理的后台 ROS 会话
4. 在同一个 master 上重复启动第二套冲突的 `roslaunch`

若需要后台启动，必须先通过：

```text
check_ros_master.sh
clean_ros_sessions.sh
```

确认不会污染旧会话。

### 5. 启动前先查、再清、后起

建议固定顺序：

1. `check_ros_master.sh`
2. `clean_ros_sessions.sh`
3. `use_cleanscout_pi.sh`
4. `start_slam_mapping.sh` 或其他唯一入口脚本

### 6. 当前正常 ROS 启动模型

当前冻结的正常模式为：

```text
一个入口脚本 -> 一次 roslaunch -> 由 roslaunch 自己管理 master
```

明确禁止的旧模式为：

```text
先手工起 roscore
再起第二套 roslaunch
再在脚本里后台补 master
```

本轮已实际验证：

1. `start_slam_mapping.sh` 单入口模式
2. 直接拉起 `/scan /imu/data /odom /map /tf`

这说明这条启动模式比旧的“多入口拼接 + 后台 master”稳定得多。

### 7. 旧入口保留但不得直接混用

以下只保留为历史参考：

1. `bringup.launch`
2. 旧 `goolge_ws`
3. 旧 `~/catkin_ws`

不得与当前新工作区唯一入口混用。

## 当前多机通信口径

### 树莓派

```bash
export ROS_MASTER_URI=http://10.11.191.84:11311
export ROS_IP=10.11.191.84
export ROS_HOSTNAME=10.11.191.84
```

### 虚拟机

```bash
export ROS_MASTER_URI=http://10.11.191.84:11311
export ROS_IP=10.11.191.128
```

## 当前工程状态结论

### 已打通

1. `/scan`
2. `/imu/data`
3. `/raw_vel`
4. `/raw_odom`
5. `/odom`
6. `/map`
7. 分布式 RViz 接入

### 当前未完全收口

当前建图链仍存在稳定性问题，尤其是：

1. 旧会话污染后会导致 `11311` 冲突
2. 重复启动会导致同名节点互相覆盖
3. 当前雷达链路在某些会话里存在中途停止刷新的现象

因此本轮冻结结论不是“地图已正式完成”，而是：

```text
唯一入口已经确定，建图前主链已打通，但运行稳定性仍在继续收敛中。
```

## 旧会话根因查明

本轮已确认：

1. 当前反复出现的“旧 rosmaster 仍在”并不是旧仓库自动启动
2. 真实根因是此前调试阶段用后台 `nohup ... roscore/rosmaster ... &` 启动过 master
3. 这些进程在父 shell 退出后变成了 `PPID=1` 的孤儿进程，继续占用 `11311`
4. 新终端由于旧 `.bashrc` 自动设置 `ROS_MASTER_URI=http://当前IP:11311`，会立刻连到这些残留 master

因此：

```text
/rosout 和 /rosout_agg 不是问题本身，
它们只是“旧 master 还活着”的表象。
```

## 本轮 runtime 整改

### 已移除

从 `~/.bashrc` 中移除：

1. `~/goolge_ws/install_isolated/setup.bash`
2. `~/catkin_ws/devel/setup.bash`
3. 自动设置 `ROS_MASTER_URI / ROS_IP / ROS_HOSTNAME`
4. 旧 `CLBBASE / CLBLIDAR / USB_CAMERA / CLB_SERIAL` 的全局默认导出

### 新增

当前统一改为通过新工作区入口进入：

```text
Raspberrypi/catkin_ws/use_cleanscout_pi.sh
```

并新增：

```text
Raspberrypi/catkin_ws/check_ros_master.sh
Raspberrypi/catkin_ws/start_slam_mapping.sh
```

作用分别为：

1. `use_cleanscout_pi.sh`
   - 只加载 `CleanScout` 新工作区
   - 按当前 IP 生成本轮 `ROS_MASTER_URI / ROS_IP / ROS_HOSTNAME`

2. `check_ros_master.sh`
   - 检查当前 shell 实际连的是谁
   - 检查是否有残留 `rosmaster / roscore / roslaunch`
   - 检查 `11311` 是否被占用

3. `start_slam_mapping.sh`
   - 先进入 `CleanScout` 环境
   - 再清理旧会话
   - 最后前台启动唯一建图入口

## 一句话总结

`C-2.3.5A` 当前冻结的是“唯一入口和唯一清理动作”，目的是把树莓派端从反复撞旧会话和旧入口的混乱状态里拉出来，为正式建图、保存地图、定位导航提供唯一权威主线。

当前进一步冻结后的长期约束是：

```text
系统级 shell 只保留 ROS 本体，项目级环境一律从 CleanScout 新工作区入口显式进入；
任何会话在启动前必须先查残留 master、先清旧会话，再起唯一入口。
```

## 本轮导航实验结果

### 已完成的正向结果

本轮基于已保存地图：

```text
Raspberrypi/maps/desk_map_001.yaml
```

已经补出并实际启动：

```text
desk_map_navigation.launch
start_desk_navigation.sh
```

当前导航链已接通到以下层级：

1. `map_server`
2. `amcl`
3. `move_base`
4. `rplidarNode`
5. `clb_base_node`
6. `ekf_localization`
7. `csrpi_cmdvel_to_wheels`
8. `csrpi_wheel_bridge`

并已实际确认：

1. `rviz` 可看到地图、激光、粒子云
2. 可点击 `2D Pose Estimate`
3. 可点击 `2D Nav Goal`
4. `move_base` 已能接受目标

### 本轮核心现象

虽然导航链已接通，但当前实车行为仍明显不正常。

现场现象为：

1. 点一个实际可到达的近目标时，小车会跑得非常快
2. 甚至可能一下冲出地图边界
3. 手动轻微转动轮子时，地图中的位移变化也明显偏大

### 当前技术判断

本轮冻结判断是：

```text
导航入口与链路已经接通，当前主问题不再是“起不来”，
而是底盘里程计 / 编码器尺度明显异常，导致定位与导航尺度失真。
```

也就是说，当前更像是：

1. `UNO` 编码器回传尺度异常
2. 或 `raw_vel/raw_odom` 换算尺度异常
3. 或两者叠加，造成 `/odom` 放大

这也解释了为什么：

1. 手拨轮子时，地图位移会过大
2. 点很近的导航点，车却会冲得很猛

### 当前结论

所以本轮的准确结论不是“导航已经收口完成”，而是：

1. 建图后定位导航主链已经打通
2. 可以进入目标点实验
3. 但当前不适合直接进入正式地面自主导航
4. 下一轮优先事项应转为：
   - `UNO/编码器回传尺度`
   - `raw_vel/raw_odom` 换算系数
   - 导航速度与里程计尺度收敛

### 给项目负责人的当前口径

可直接反馈为：

```text
树莓派侧当前已经把地图、AMCL、move_base、底盘桥接通到可点 2D Nav Goal 的程度；
但本轮实测表明里程计/编码器尺度明显异常，导致车辆运动幅度远大于地图中的合理尺度，当前暂不宜直接进入正式导航验收。
```
