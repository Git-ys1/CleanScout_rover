# C-2.3.2 Distributed RViz Prepare

## 文档定位

- 任务：`C-2.3.2A-Plus`
- 平台：树莓派作为 ROS 主机，Ubuntu 20.04 虚拟机作为 RViz 观察端

## 当前网络口径

### 树莓派

- IP：`10.11.191.84`
- 角色：`rosmaster + 传感器节点 + 底盘桥节点`

### 虚拟机

- IP：`10.11.191.128`
- 角色：`RViz 观察端`

## 当前树莓派端环境变量

树莓派使用：

```bash
export ROS_MASTER_URI=http://10.11.191.84:11311
export ROS_IP=10.11.191.84
export ROS_HOSTNAME=10.11.191.84
```

## 当前虚拟机端环境变量

虚拟机使用：

```bash
export ROS_MASTER_URI=http://10.11.191.84:11311
export ROS_IP=10.11.191.128
```

## 当前树莓派端 bringup

本轮树莓派端已按分布式口径启动：

1. `rosmaster --core`
2. `roslaunch clbrobot bench_full_stack.launch use_rviz:=false`

## 当前应可见的话题

在虚拟机侧应至少可见：

1. `/scan`
2. `/imu/data`
3. `/tf`
4. `/csr_base/raw_serial_line`
5. `/csr_base/encoder_debug`
6. `/csr_base/pid_debug`
7. `/csr_base/bridge_status`

## 当前虚拟机侧观察结果

本轮已现场确认：

1. 虚拟机已成功连到树莓派 ROS master
2. `RViz` 已成功看到雷达扫描结果
3. `RViz` 已成功看到 IMU 的 3D 箭头

现场反馈为：

```text
完美！完全可以看到雷达的扫描结果和IMU的3D的箭头！！
```

## 当前结论

当前 `C-2.3.2A-Plus` 可判定通过：

1. 树莓派端负责 ROS 主机和传感器栈
2. 虚拟机端成功作为 RViz 观察机接入
3. 分布式观察链路已打通

## 一句话总结

本轮已经证明：

```text
树莓派可以稳定承载整机 ROS 栈，而 Ubuntu 20.04 虚拟机可以作为分布式 RViz 观察端，成功查看 /scan 与 /imu/data 的可视化结果。
```
