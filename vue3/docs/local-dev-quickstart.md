# 本地联调快捷启动

本文件不再维护独立部署口径。

`V-1.8.9` 起，`vue3/` 的本地联调、前端构建、云端后端、Netlify H5、edge-relay 与 OpenClaw 交接统一查看：

```text
docs/deployment.md
```

快捷启动命令仍为：

```powershell
cd F:\Project\CSc——uniapp\vue3
cmd /c npm.cmd run local:edge
```
